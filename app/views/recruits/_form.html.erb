<%= simple_form_for(@recruit) do |f|  %>
  <%= f.error_notification %>
  <div class="field">
  <div class="control">
  <%= f.input :recruit, label: "Recruit about it", input_html: { class: "textarea " }, wrapper: false, label_html: {class: "label" }, placeholder: "Compose a new conversation...recruit ",  autofocus: true %>
  </div>
  </div>
  <%= f.button :submit, class: "button is-info" %>
  <% end %>
